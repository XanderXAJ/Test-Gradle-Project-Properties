# Test Gradle Project Properties

A little test to experiment with how project properties (passed to Gradle via `-P`) work.

## Run

```
./gradlew
./gradlew -Pbranch=master
./gradlew -Pbranch=a_branch
```